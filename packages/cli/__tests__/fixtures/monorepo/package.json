{
  "private": true,
  "version": "1.0.0",
  "description": "custom helpers",
  "type": "module",
  "scripts": {
    "cli": "../../../../../node_modules/.bin/tsx ../../../src/index.ts",
    "build": "npm run cli -- build",
    "test": "npm run cli -- test --package-name '*'",
    "release": "npm run cli -- release"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deot/dev-demo.git"
  },
  "devDependencies": {
    "@deot/dev-cli": "*"
  }
}
