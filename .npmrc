# 依赖提升设置. 
# public-hoist-pattern[] = *types* # __tests__
# public-hoist-pattern[] = *eslint*
# public-hoist-pattern[] = *inquirer* # packages/test/__tests__/fixtures
# public-hoist-pattern[] = husky
# public-hoist-pattern[] = jest
# public-hoist-pattern[] = lint-staged
# public-hoist-pattern[] = cross-env
# 
# 以上配置pnpm install生效, npx pnpm install 或 npm run init上面配置读取成string类型了（pnpm@8.2.0）
shamefully-hoist = true
